<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" href="/images/outsourced/LOGO.png" type="image/png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .news-detail {
            background-color: var(--white);
            border-radius: 8px;
            overflow: hidden;
            box-shadow: var(--shadow);
            margin-bottom: 40px;
        }
        .news-detail-image {
            max-width: 500px;
            max-height: 500px;
            margin: 0 auto;
            overflow: hidden;
        }
        .news-detail-image img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }
        .news-detail-content {
            padding: 30px;
        }
        .news-meta {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 20px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--light);
        }
        .news-meta-item {
            display: flex;
            align-items: center;
            color: var(--text-light);
        }
        .news-meta-item i {
            margin-right: 8px;
            color: var(--primary);
        }
        .news-content {
            margin-bottom: 30px;
            line-height: 1.7;
        }
        .news-tags {
            margin-bottom: 30px;
        }
        .news-tags h4 {
            margin-bottom: 10px;
            color: var(--primary);
        }
        .tag-list {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }
        .tag-item {
            background-color: var(--light);
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.9rem;
            color: var(--primary);
        }
        .news-actions {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }
        .news-stats {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
        }
        .stat-item {
            display: flex;
            align-items: center;
            color: var(--text-light);
        }
        .stat-item i {
            margin-right: 5px;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <section class="section">
        <div class="container">
            <div class="news-detail">
                <% if (news.image) { %>
                    <div class="news-detail-image">
                        <img src="<%= news.image %>" alt="<%= news.title %>">
                    </div>
                <% } %>
                <% if (news.video) { %>
                    <div class="news-detail-image">
                        <video controls style="width: 100%; max-height: 500px;">
                            <source src="<%= news.video %>" type="video/mp4">
                            Your browser does not support the video tag.
                        </video>
                    </div>
                <% } %>
                <div class="news-detail-content">
                    <h1><%= news.title %></h1>

                    <div class="news-meta">
                        <div class="news-meta-item">
                            <i class="fas fa-calendar"></i>
                            <%= new Date(news.publishedDate || news.date).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %>
                        </div>
                        <div class="news-meta-item">
                            <i class="fas fa-tag"></i>
                            <%= news.category.charAt(0).toUpperCase() + news.category.slice(1) %>
                        </div>
                        <% if (news.authorName || (news.author && news.author.name)) { %>
                            <div class="news-meta-item">
                                <i class="fas fa-user"></i>
                                By <%= news.authorName || news.author.name %>
                            </div>
                        <% } %>
                        <% if (news.authorRole) { %>
                            <div class="news-meta-item">
                                <i class="fas fa-briefcase"></i>
                                <%= news.authorRole %>
                            </div>
                        <% } %>
                    </div>

                    <div class="news-stats">
                        <div class="stat-item">
                            <i class="fas fa-eye"></i>
                            <%= news.views %> views
                        </div>
                        <div class="stat-item">
                            <i class="fas fa-heart"></i>
                            <%= news.likes.length %> likes
                        </div>
                    </div>

                    <div class="news-content">
                        <%= news.content.replace(/\n/g, '<br>') %>
                    </div>

                    <% if (news.tags && news.tags.length > 0) { %>
                        <div class="news-tags">
                            <h4>Tags</h4>
                            <div class="tag-list">
                                <% news.tags.forEach(tag => { %>
                                    <div class="tag-item"><%= tag %></div>
                                <% }) %>
                            </div>
                        </div>
                    <% } %>

                    <div class="news-actions">
                        <a href="/news" class="btn">Back to News</a>
                        <% if (user) { %>
                            <% const hasLiked = news.likes.some(userId => userId.equals(user._id)); %>
                            <form action="/news/<%= news._id %>/like" method="POST" style="display: inline;">
                                <button type="submit" class="btn <%= hasLiked ? 'btn-accent' : '' %>" style="<%= hasLiked ? '' : 'background-color: #28a745;' %>">
                                    <i class="fas fa-heart"></i> <%= hasLiked ? 'Unlike' : 'Like' %> (<%= news.likes.length %>)
                                </button>
                            </form>
                        <% } %>
                        <% if (user && user.role === 'admin') { %>
                            <a href="/news/<%= news._id %>/edit" class="btn" style="background-color: #28a745;">Edit Article</a>
                            <form action="/news/<%= news._id %>?_method=DELETE" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this article?')">
                                <button type="submit" class="btn" style="background-color: #dc3545;">Delete Article</button>
                            </form>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <%- include('../partials/footer') %>

    <script src="/js/script.js"></script>
</body>
</html>
